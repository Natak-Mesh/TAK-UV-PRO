<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Camera (QR scanning) -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Bluetooth permissions -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />


    <uses-feature android:name="android.hardware.bluetooth" android:required="true" />

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:icon="@drawable/ic_uvpro"
        android:roundIcon="@drawable/ic_uvpro">

        <meta-data android:name="plugin-api" android:value="${atakApiVersion}" />

        <!-- QR scan trampoline for channel import -->
        <activity
            android:name="com.uvpro.plugin.QrScanActivity"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:exported="true" />

        <!-- Cross-process QR result IPC (plugin process → ATAK process) -->
        <provider
            android:name="com.uvpro.plugin.QrResultProvider"
            android:authorities="com.uvpro.plugin.qrresult"
            android:exported="true" />

        <!-- Allow ATAK plugin discovery -->
        <activity android:name="com.atakmap.app.component"
            android:exported="true"
            tools:ignore="MissingClass">
            <intent-filter android:label="@string/app_name">
                <action android:name="com.atakmap.app.component" />
            </intent-filter>
        </activity>

    </application>
</manifest>
